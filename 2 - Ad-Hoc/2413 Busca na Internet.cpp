// this problem doesn't have a english version

#include <iostream>

int main()
{
    int clicks_third_link{};
    std::cin >> clicks_third_link;

    int clicks_first_link{ clicks_third_link * 4 };
    std::cout << clicks_first_link << '\n';
    return 0;
}